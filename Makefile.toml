[tasks.image]
command = "docker"
args = ["build", "-t", "mysql-rust-pinger", "."]

[tasks.tag]
script = [
]

[tasks.push]
script = [
]

[tasks.install]
command = "cargo"
args = ["install", "--path", "."]

[tasks.package]
command = "cargo"
args = ["build", "--release"]

[tasks.version]
script = [
	"echo ${VERSION:? required}"
]

[tasks.precommit]
script = [
    "cargo fmt",
	# "cargo clippy"
]

[tasks.publish]
dependencies = [
	"version",
	# "image",
	# "tag",
	# "push",
    "precommit",
	"package"
]